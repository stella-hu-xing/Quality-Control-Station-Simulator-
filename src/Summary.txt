现存问题：

1. check all sleeping methods                                                 (get!)

2. try to change all while loop into wait() and notify() methods..            (get!)
    1) insepctor as a monitor,while robot using it
    2) short belt ..need to wait .. 
    
3. manage all output and code Layout                                          (get!)

4. change all using of direct variable to Set() and Get() methods             (get!)

5. finish all comments and reflection

6. all subclass need to add super() into all overrided methods                (get!)

7. makefile                                                                    (get!)

8. delete all packages                                                         (get!)

9. make some variable to be volatile                                           (get!)

10. add more exception                                                         (get!)